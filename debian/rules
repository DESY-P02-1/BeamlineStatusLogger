#! /usr/bin/make -f

export DH_VERBOSE = 1
export PYBUILD_NAME = python3-beamline-status-logger

#export PYBUILD_TEST_PYTEST = 1
export PYBUILD_DISABLE=test

%:
	dh $@ --with python3 --buildsystem=pybuild
